import React from 'react';
import styles from './Main.module.scss'


const Main = () => (
    <div className={styles.container}>
        <h1>Select a thread or start a new conversation</h1>
    </div>
);

export default Main;